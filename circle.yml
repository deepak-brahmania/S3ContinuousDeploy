dependencies:
    override:
        - sudo pip install awscli
deployment:
  stage:
    branch: master
    commands:
      - aws s3 sync /home/ubuntu/S3ContinuousDeploy s3://decanter-circleci-test --delete
